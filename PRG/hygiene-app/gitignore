# Dependencies
**/node_modules
.pnpm-store/

# Build outputs
dist/
build/
*.dist

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# IDE and editor files
.vscode/
.idea/
*.swp
*.swo
*~

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Runtime data
pids
*.pid
*.seed
*.pid.lock
*.bak

# Coverage directory used by tools like istanbul
coverage/
*.lcov

# nyc test coverage
.nyc_output

# Dependency directories
jspm_packages/

# TypeScript cache
*.tsbuildinfo

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Microbundle cache
.rpt2_cache/
.rts2_cache_cjs/
.rts2_cache_es/
.rts2_cache_umd/

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# parcel-bundler cache (https://parceljs.org/)
.cache
.parcel-cache

# Next.js build output
.next

# Nuxt.js build / generate output
.nuxt

# Gatsby files
.cache/

# Storybook build outputs
.out
.storybook-out

# Temporary folders
tmp/
temp/

# Database
*.db
*.sqlite
*.sqlite3

# Webdev artifacts (checkpoint zips, migrations, etc.)
.webdev/

# Manus version file (auto-generated, not part of source)
client/public/__manus__/version.json

# =============================================================================
# HygieneCheck – Projektspezifische Einträge
# =============================================================================

# Umgebungsvariablen (NIEMALS einchecken!)
# Die .env-Datei enthält Datenbankpasswörter, JWT-Secrets und API-Keys.
# Vorlage: env-template.txt
.env.*
!.env.example
*.pem
*.key
*.crt
*.p12
*.pfx

# Drizzle-Migrationen (generierte SQL-Dateien)
# Werden aus drizzle/schema.ts neu generiert: pnpm drizzle-kit generate
drizzle/*.sql
drizzle/meta/

# Docker / Podman lokale Overrides
# Lokale Überschreibungen mit Passwortangaben nicht einchecken
docker-compose.override.yml
docker-compose.local.yml
docker/volumes/

# Datei-Uploads (werden in MinIO/S3 gespeichert)
uploads/
storage/local/

# Manus-spezifische Laufzeitdateien
.manus-logs/
screenshots/
.manus/

# Betriebssystem (Windows)
Desktop.ini
$RECYCLE.BIN/
*.lnk

# Betriebssystem (Linux)
.fuse_hidden*
.directory
.Trash-*
.nfs*
